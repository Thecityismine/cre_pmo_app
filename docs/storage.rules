rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Authenticated users can read/write project documents
    match /projects/{projectId}/documents/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
